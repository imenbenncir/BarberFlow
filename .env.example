# Environment Variables — BarberFlow

## Server (server/.env)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/barberflow
JWT_SECRET=your_super_secret_jwt_key_here
PORT=5099

## Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

## Vercel Production
# CLIENT_URL = URL of your deployed Vercel frontend, e.g.:
CLIENT_URL=https://barber-flow.vercel.app
NODE_ENV=production

## Client (client/.env)
# In production on Vercel, leave empty — the proxy rewrite handles /api routing.
# Only needed if you deploy client and server separately:
# VITE_API_URL=https://barber-flow.vercel.app/api
